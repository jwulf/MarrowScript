// Generated by MarrowScript compiler. Import this file into your frontend.
// System: InventoryPlatform v1.0.0

export interface Player {
  id: string;
  created_at: string;
  updated_at: string;
  username: string;
  email: string;
  score: number;
  level: number;
  inventory: unknown[];
}

export interface Item {
  id: string;
  created_at: string;
  updated_at: string;
  name: string;
  quantity: number;
  rarity: string;
  tradeable: boolean;
  owner_id: string;
  metadata: unknown;
}

export interface TradeOffer {
  id: string;
  created_at: string;
  updated_at: string;
  from_player: string;
  to_player: string;
  offered_items: string[];
  requested_items: string[];
  status: string;
  expires_at: string;
}

export class InventoryPlatformClient {
  constructor(private baseUrl: string, private getToken?: () => string | null) {}

  private async request<T>(method: string, path: string, body?: unknown): Promise<T> {
    const headers: Record<string, string> = {
      "Content-Type": "application/json",
    };
    if (this.getToken) {
      const token = this.getToken();
      if (token) headers["Authorization"] = "Bearer " + token;
    }
    const res = await fetch(this.baseUrl + path, {
      method,
      headers,
      body: body !== undefined ? JSON.stringify(body) : undefined,
    });
    if (!res.ok) {
      const text = await res.text().catch(() => res.statusText);
      throw new Error(`HTTP ${res.status}: ${text}`);
    }
    if (res.status === 204) return undefined as unknown as T;
    return res.json() as Promise<T>;
  }

  // ─── Player ───

  listPlayer(page = 1, pageSize = 50): Promise<{ items: Player[]; total: number; page: number; page_size: number }> {
    return this.request("GET", `/players?page=${page}&page_size=${pageSize}`);
  }

  getPlayer(id: string): Promise<Player> {
    return this.request("GET", `/players/${id}`);
  }

  createPlayer(data: Partial<Player>): Promise<Player> {
    return this.request("POST", "/players", data);
  }

  updatePlayer(id: string, data: Partial<Player>): Promise<Player> {
    return this.request("PUT", `/players/${id}`, data);
  }

  deletePlayer(id: string): Promise<void> {
    return this.request("DELETE", `/players/${id}`);
  }

  createTrade(data: Record<string, unknown>): Promise<{ ok: boolean; action: string }> {
    return this.request("POST", "/players/create-trade", data);
  }

  acceptTrade(data: Record<string, unknown>): Promise<{ ok: boolean; action: string }> {
    return this.request("POST", "/players/accept-trade", data);
  }

  awardXp(data: Record<string, unknown>): Promise<{ ok: boolean; action: string }> {
    return this.request("POST", "/players/award-xp", data);
  }

  consumeItem(data: Record<string, unknown>): Promise<{ ok: boolean; action: string }> {
    return this.request("POST", "/players/consume-item", data);
  }

  // ─── Item ───

  listItem(page = 1, pageSize = 50): Promise<{ items: Item[]; total: number; page: number; page_size: number }> {
    return this.request("GET", `/items?page=${page}&page_size=${pageSize}`);
  }

  getItem(id: string): Promise<Item> {
    return this.request("GET", `/items/${id}`);
  }

  createItem(data: Partial<Item>): Promise<Item> {
    return this.request("POST", "/items", data);
  }

  updateItem(id: string, data: Partial<Item>): Promise<Item> {
    return this.request("PUT", `/items/${id}`, data);
  }

  deleteItem(id: string): Promise<void> {
    return this.request("DELETE", `/items/${id}`);
  }

  consumeItem(data: Record<string, unknown>): Promise<{ ok: boolean; action: string }> {
    return this.request("POST", "/items/consume-item", data);
  }

  // ─── TradeOffer ───

  listTradeOffer(page = 1, pageSize = 50): Promise<{ items: TradeOffer[]; total: number; page: number; page_size: number }> {
    return this.request("GET", `/trade_offers?page=${page}&page_size=${pageSize}`);
  }

  getTradeOffer(id: string): Promise<TradeOffer> {
    return this.request("GET", `/trade_offers/${id}`);
  }

  createTradeOffer(data: Partial<TradeOffer>): Promise<TradeOffer> {
    return this.request("POST", "/trade_offers", data);
  }

  updateTradeOffer(id: string, data: Partial<TradeOffer>): Promise<TradeOffer> {
    return this.request("PUT", `/trade_offers/${id}`, data);
  }

  deleteTradeOffer(id: string): Promise<void> {
    return this.request("DELETE", `/trade_offers/${id}`);
  }

  acceptTrade(data: Record<string, unknown>): Promise<{ ok: boolean; action: string }> {
    return this.request("POST", "/trade_offers/accept-trade", data);
  }

}
