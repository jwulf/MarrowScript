// Generated by MarrowScript compiler. DO NOT EDIT.

export type ItemState = "available" | "reserved" | "consumed" | "deleted";

export const ITEM_INITIAL: ItemState = "available";

const TRANSITIONS: Record<ItemState, Record<string, ItemState>> = {
  "available": { "available_to_reserved": "reserved" },
  "reserved": { "reserved_to_consumed": "consumed" },
  "consumed": { "consumed_to_deleted": "deleted" },
  "deleted": {  },
};

export function transitionItem(
  current: ItemState,
  trigger: string
): { ok: true; next: ItemState } | { ok: false; error: string } {
  const next = TRANSITIONS[current]?.[trigger];
  if (!next) return { ok: false, error: `Invalid transition: ${current} --[${trigger}]--> ?` };
  return { ok: true, next };
}
