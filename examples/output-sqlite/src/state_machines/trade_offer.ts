// Generated by MarrowScript compiler. DO NOT EDIT.

export type TradeOfferState = "pending" | "accepted" | "rejected" | "expired" | "cancelled";

export const TRADEOFFER_INITIAL: TradeOfferState = "pending";

const TRANSITIONS: Record<TradeOfferState, Record<string, TradeOfferState>> = {
  "pending": { "pending_to_accepted": "accepted" },
  "accepted": { "accepted_to_rejected": "rejected", "accepted_to_expired": "expired", "accepted_to_cancelled": "cancelled" },
  "rejected": {  },
  "expired": {  },
  "cancelled": {  },
};

export function transitionTradeOffer(
  current: TradeOfferState,
  trigger: string
): { ok: true; next: TradeOfferState } | { ok: false; error: string } {
  const next = TRANSITIONS[current]?.[trigger];
  if (!next) return { ok: false, error: `Invalid transition: ${current} --[${trigger}]--> ?` };
  return { ok: true, next };
}
