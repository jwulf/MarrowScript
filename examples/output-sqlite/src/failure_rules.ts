// Generated by MarrowScript compiler. DO NOT EDIT.
// Implements spec/10 Â§6 (Failure Rules)
//
// Each rule has: condition, detection, remediation, escalation.
// Rules are derived from the program's constraints, state machines,
// dependencies, events, and flows. All rule-based â€” no ML.

import { logger } from "./logger";
import { counter, gauge } from "./metrics";

export interface FailureRule {
  id: string;
  condition: string;
  detection: string;
  remediation: () => Promise<void>;
  rollback: boolean;
}

export const RULES: FailureRule[] = [
  {
    id: "invalid_transition_Player",
    condition: "Attempted state transition not in declared machine",
    detection: "Log with error_code=INVALID_TRANSITION",
    remediation: async () => {
      counter("constraint.violated", { entity: "Player", kind: "invalid_transition" });
      logger.warn("invalid_transition_blocked", { entity_id: "Player", error_code: "INVALID_TRANSITION" });
    },
    rollback: false,
  },
  {
    id: "invalid_transition_Item",
    condition: "Attempted state transition not in declared machine",
    detection: "Log with error_code=INVALID_TRANSITION",
    remediation: async () => {
      counter("constraint.violated", { entity: "Item", kind: "invalid_transition" });
      logger.warn("invalid_transition_blocked", { entity_id: "Item", error_code: "INVALID_TRANSITION" });
    },
    rollback: false,
  },
  {
    id: "invalid_transition_TradeOffer",
    condition: "Attempted state transition not in declared machine",
    detection: "Log with error_code=INVALID_TRANSITION",
    remediation: async () => {
      counter("constraint.violated", { entity: "TradeOffer", kind: "invalid_transition" });
      logger.warn("invalid_transition_blocked", { entity_id: "TradeOffer", error_code: "INVALID_TRANSITION" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_PlayerService_to_PlayerStore",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "PlayerService", target: "PlayerStore" });
      gauge("circuit_breaker.open", 1, { service: "PlayerService", target: "PlayerStore" });
      logger.error("circuit_breaker_open", { service: "PlayerService" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_ItemService_to_ItemStore",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "ItemService", target: "ItemStore" });
      gauge("circuit_breaker.open", 1, { service: "ItemService", target: "ItemStore" });
      logger.error("circuit_breaker_open", { service: "ItemService" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_APIGateway_to_PlayerService",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "APIGateway", target: "PlayerService" });
      gauge("circuit_breaker.open", 1, { service: "APIGateway", target: "PlayerService" });
      logger.error("circuit_breaker_open", { service: "APIGateway" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_APIGateway_to_ItemService",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "APIGateway", target: "ItemService" });
      gauge("circuit_breaker.open", 1, { service: "APIGateway", target: "ItemService" });
      logger.error("circuit_breaker_open", { service: "APIGateway" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_APIGateway_to_TradeOfferService",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "APIGateway", target: "TradeOfferService" });
      gauge("circuit_breaker.open", 1, { service: "APIGateway", target: "TradeOfferService" });
      logger.error("circuit_breaker_open", { service: "APIGateway" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_APIGateway_to_game_lobby",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "APIGateway", target: "game_lobby" });
      gauge("circuit_breaker.open", 1, { service: "APIGateway", target: "game_lobby" });
      logger.error("circuit_breaker_open", { service: "APIGateway" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_APIGateway_to_trade_notifications",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "APIGateway", target: "trade_notifications" });
      gauge("circuit_breaker.open", 1, { service: "APIGateway", target: "trade_notifications" });
      logger.error("circuit_breaker_open", { service: "APIGateway" });
    },
    rollback: false,
  },
  {
    id: "event_delivery_failure_ItemTraded",
    condition: "Event not acknowledged within TTL",
    detection: "Event age > 604800000 ms",
    remediation: async () => {
      counter("event.delivery_failed", { event: "ItemTraded" });
      logger.error("event_delivery_failed", { event: "ItemTraded", error_code: "DELIVERY_TIMEOUT" });
    },
    rollback: true,
  },
  {
    id: "event_delivery_failure_PlayerLeveledUp",
    condition: "Event not acknowledged within TTL",
    detection: "Event age > 2592000000 ms",
    remediation: async () => {
      counter("event.delivery_failed", { event: "PlayerLeveledUp" });
      logger.error("event_delivery_failed", { event: "PlayerLeveledUp", error_code: "DELIVERY_TIMEOUT" });
    },
    rollback: false,
  },
  {
    id: "event_delivery_failure_TradeOfferCreated",
    condition: "Event not acknowledged within TTL",
    detection: "Event age > 604800000 ms",
    remediation: async () => {
      counter("event.delivery_failed", { event: "TradeOfferCreated" });
      logger.error("event_delivery_failed", { event: "TradeOfferCreated", error_code: "DELIVERY_TIMEOUT" });
    },
    rollback: false,
  },
  {
    id: "flow_step_failure_execute_trade_validate",
    condition: "Flow step failed after max retries",
    detection: "Step returns error after retry policy exhausted",
    remediation: async () => {
      counter("flow.step_failed", { flow: "execute_trade", step: "validate" });
      // Trigger compensation: cancel_trade(offer)
      logger.error("flow_step_failed", { event: "execute_trade.validate", error_code: "STEP_FAILED" });
    },
    rollback: true,
  },
  {
    id: "flow_step_failure_execute_trade_lock_items",
    condition: "Flow step failed after max retries",
    detection: "Step returns error after retry policy exhausted",
    remediation: async () => {
      counter("flow.step_failed", { flow: "execute_trade", step: "lock_items" });
      // Trigger compensation: release_items(offer.items)
      logger.error("flow_step_failed", { event: "execute_trade.lock_items", error_code: "STEP_FAILED" });
    },
    rollback: true,
  },
  {
    id: "flow_step_failure_execute_trade_transfer",
    condition: "Flow step failed after max retries",
    detection: "Step returns error after retry policy exhausted",
    remediation: async () => {
      counter("flow.step_failed", { flow: "execute_trade", step: "transfer" });
      // Trigger compensation: revert_transfer(offer)
      logger.error("flow_step_failed", { event: "execute_trade.transfer", error_code: "STEP_FAILED" });
    },
    rollback: true,
  },
  {
    id: "flow_step_failure_execute_trade_notify",
    condition: "Flow step failed after max retries",
    detection: "Step returns error after retry policy exhausted",
    remediation: async () => {
      counter("flow.step_failed", { flow: "execute_trade", step: "notify" });
      // Trigger compensation: emit_trade_reverted(offer)
      logger.error("flow_step_failed", { event: "execute_trade.notify", error_code: "STEP_FAILED" });
    },
    rollback: true,
  },
];

export function findRule(id: string): FailureRule | undefined {
  return RULES.find(r => r.id === id);
}

export async function triggerRule(id: string): Promise<void> {
  const rule = findRule(id);
  if (!rule) {
    logger.warn("unknown_rule_triggered", { event: "rule_lookup", error_code: "UNKNOWN_RULE", metadata: { id } });
    return;
  }
  try {
    await rule.remediation();
  } catch (e: any) {
    logger.error("remediation_failed", { event: id, error_code: "REMEDIATION_FAILED", metadata: { error: e.message } });
  }
}