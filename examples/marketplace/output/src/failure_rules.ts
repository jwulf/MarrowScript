// Generated by MarrowScript compiler. DO NOT EDIT.
// Implements spec/10 Â§6 (Failure Rules)
//
// Each rule has: condition, detection, remediation, escalation.
// Rules are derived from the program's constraints, state machines,
// dependencies, events, and flows. All rule-based â€” no ML.

import { logger } from "./logger";
import { counter, gauge } from "./metrics";

export interface FailureRule {
  id: string;
  condition: string;
  detection: string;
  remediation: () => Promise<void>;
  rollback: boolean;
}

export const RULES: FailureRule[] = [
  {
    id: "invalid_transition_Seller",
    condition: "Attempted state transition not in declared machine",
    detection: "Log with error_code=INVALID_TRANSITION",
    remediation: async () => {
      counter("constraint.violated", { entity: "Seller", kind: "invalid_transition" });
      logger.warn("invalid_transition_blocked", { entity_id: "Seller", error_code: "INVALID_TRANSITION" });
    },
    rollback: false,
  },
  {
    id: "invalid_transition_Listing",
    condition: "Attempted state transition not in declared machine",
    detection: "Log with error_code=INVALID_TRANSITION",
    remediation: async () => {
      counter("constraint.violated", { entity: "Listing", kind: "invalid_transition" });
      logger.warn("invalid_transition_blocked", { entity_id: "Listing", error_code: "INVALID_TRANSITION" });
    },
    rollback: false,
  },
  {
    id: "invalid_transition_Buyer",
    condition: "Attempted state transition not in declared machine",
    detection: "Log with error_code=INVALID_TRANSITION",
    remediation: async () => {
      counter("constraint.violated", { entity: "Buyer", kind: "invalid_transition" });
      logger.warn("invalid_transition_blocked", { entity_id: "Buyer", error_code: "INVALID_TRANSITION" });
    },
    rollback: false,
  },
  {
    id: "invalid_transition_Order",
    condition: "Attempted state transition not in declared machine",
    detection: "Log with error_code=INVALID_TRANSITION",
    remediation: async () => {
      counter("constraint.violated", { entity: "Order", kind: "invalid_transition" });
      logger.warn("invalid_transition_blocked", { entity_id: "Order", error_code: "INVALID_TRANSITION" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_SellerService_to_SellerStore",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "SellerService", target: "SellerStore" });
      gauge("circuit_breaker.open", 1, { service: "SellerService", target: "SellerStore" });
      logger.error("circuit_breaker_open", { service: "SellerService" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_ListingService_to_ListingStore",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "ListingService", target: "ListingStore" });
      gauge("circuit_breaker.open", 1, { service: "ListingService", target: "ListingStore" });
      logger.error("circuit_breaker_open", { service: "ListingService" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_BuyerService_to_BuyerStore",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "BuyerService", target: "BuyerStore" });
      gauge("circuit_breaker.open", 1, { service: "BuyerService", target: "BuyerStore" });
      logger.error("circuit_breaker_open", { service: "BuyerService" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_OrderService_to_OrderStore",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "OrderService", target: "OrderStore" });
      gauge("circuit_breaker.open", 1, { service: "OrderService", target: "OrderStore" });
      logger.error("circuit_breaker_open", { service: "OrderService" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_APIGateway_to_SellerService",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "APIGateway", target: "SellerService" });
      gauge("circuit_breaker.open", 1, { service: "APIGateway", target: "SellerService" });
      logger.error("circuit_breaker_open", { service: "APIGateway" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_APIGateway_to_ListingService",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "APIGateway", target: "ListingService" });
      gauge("circuit_breaker.open", 1, { service: "APIGateway", target: "ListingService" });
      logger.error("circuit_breaker_open", { service: "APIGateway" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_APIGateway_to_BuyerService",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "APIGateway", target: "BuyerService" });
      gauge("circuit_breaker.open", 1, { service: "APIGateway", target: "BuyerService" });
      logger.error("circuit_breaker_open", { service: "APIGateway" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_APIGateway_to_OrderService",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "APIGateway", target: "OrderService" });
      gauge("circuit_breaker.open", 1, { service: "APIGateway", target: "OrderService" });
      logger.error("circuit_breaker_open", { service: "APIGateway" });
    },
    rollback: false,
  },
  {
    id: "dependency_failure_APIGateway_to_ReviewService",
    condition: "Dependency unreachable or returning errors",
    detection: "Error rate > 50% in 30s window",
    remediation: async () => {
      counter("circuit_breaker.opened", { service: "APIGateway", target: "ReviewService" });
      gauge("circuit_breaker.open", 1, { service: "APIGateway", target: "ReviewService" });
      logger.error("circuit_breaker_open", { service: "APIGateway" });
    },
    rollback: false,
  },
  {
    id: "event_delivery_failure_OrderPlaced",
    condition: "Event not acknowledged within TTL",
    detection: "Event age > 2592000000 ms",
    remediation: async () => {
      counter("event.delivery_failed", { event: "OrderPlaced" });
      logger.error("event_delivery_failed", { event: "OrderPlaced", error_code: "DELIVERY_TIMEOUT" });
    },
    rollback: false,
  },
  {
    id: "event_delivery_failure_PaymentProcessed",
    condition: "Event not acknowledged within TTL",
    detection: "Event age > 7776000000 ms",
    remediation: async () => {
      counter("event.delivery_failed", { event: "PaymentProcessed" });
      logger.error("event_delivery_failed", { event: "PaymentProcessed", error_code: "DELIVERY_TIMEOUT" });
    },
    rollback: true,
  },
  {
    id: "event_delivery_failure_OrderShipped",
    condition: "Event not acknowledged within TTL",
    detection: "Event age > 2592000000 ms",
    remediation: async () => {
      counter("event.delivery_failed", { event: "OrderShipped" });
      logger.error("event_delivery_failed", { event: "OrderShipped", error_code: "DELIVERY_TIMEOUT" });
    },
    rollback: false,
  },
  {
    id: "event_delivery_failure_OrderCancelled",
    condition: "Event not acknowledged within TTL",
    detection: "Event age > 2592000000 ms",
    remediation: async () => {
      counter("event.delivery_failed", { event: "OrderCancelled" });
      logger.error("event_delivery_failed", { event: "OrderCancelled", error_code: "DELIVERY_TIMEOUT" });
    },
    rollback: false,
  },
  {
    id: "event_delivery_failure_ReviewPosted",
    condition: "Event not acknowledged within TTL",
    detection: "Event age > 7776000000 ms",
    remediation: async () => {
      counter("event.delivery_failed", { event: "ReviewPosted" });
      logger.error("event_delivery_failed", { event: "ReviewPosted", error_code: "DELIVERY_TIMEOUT" });
    },
    rollback: false,
  },
  {
    id: "flow_step_failure_checkout_validate",
    condition: "Flow step failed after max retries",
    detection: "Step returns error after retry policy exhausted",
    remediation: async () => {
      counter("flow.step_failed", { flow: "checkout", step: "validate" });
      // Trigger compensation: cancel_order(order)
      logger.error("flow_step_failed", { event: "checkout.validate", error_code: "STEP_FAILED" });
    },
    rollback: true,
  },
  {
    id: "flow_step_failure_checkout_pay",
    condition: "Flow step failed after max retries",
    detection: "Step returns error after retry policy exhausted",
    remediation: async () => {
      counter("flow.step_failed", { flow: "checkout", step: "pay" });
      // Trigger compensation: cancel_order(order)
      logger.error("flow_step_failed", { event: "checkout.pay", error_code: "STEP_FAILED" });
    },
    rollback: true,
  },
  {
    id: "flow_step_failure_checkout_confirm",
    condition: "Flow step failed after max retries",
    detection: "Step returns error after retry policy exhausted",
    remediation: async () => {
      counter("flow.step_failed", { flow: "checkout", step: "confirm" });
      // Trigger compensation: cancel_order(order)
      logger.error("flow_step_failed", { event: "checkout.confirm", error_code: "STEP_FAILED" });
    },
    rollback: true,
  },
];

export function findRule(id: string): FailureRule | undefined {
  return RULES.find(r => r.id === id);
}

export async function triggerRule(id: string): Promise<void> {
  const rule = findRule(id);
  if (!rule) {
    logger.warn("unknown_rule_triggered", { event: "rule_lookup", error_code: "UNKNOWN_RULE", metadata: { id } });
    return;
  }
  try {
    await rule.remediation();
  } catch (e: any) {
    logger.error("remediation_failed", { event: id, error_code: "REMEDIATION_FAILED", metadata: { error: e.message } });
  }
}