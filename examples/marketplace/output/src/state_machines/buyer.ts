// Generated by MarrowScript compiler. DO NOT EDIT.

export type BuyerState = "active" | "suspended";

export const BUYER_INITIAL: BuyerState = "active";

const TRANSITIONS: Record<BuyerState, Record<string, BuyerState>> = {
  "active": { "active_to_suspended": "suspended" },
  "suspended": {  },
};

export function transitionBuyer(
  current: BuyerState,
  trigger: string
): { ok: true; next: BuyerState } | { ok: false; error: string } {
  const next = TRANSITIONS[current]?.[trigger];
  if (!next) return { ok: false, error: `Invalid transition: ${current} --[${trigger}]--> ?` };
  return { ok: true, next };
}
