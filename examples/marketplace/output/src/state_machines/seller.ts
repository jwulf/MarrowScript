// Generated by MarrowScript compiler. DO NOT EDIT.

export type SellerState = "active" | "suspended" | "banned";

export const SELLER_INITIAL: SellerState = "active";

const TRANSITIONS: Record<SellerState, Record<string, SellerState>> = {
  "active": { "active_to_suspended": "suspended" },
  "suspended": { "suspended_to_banned": "banned" },
  "banned": {  },
};

export function transitionSeller(
  current: SellerState,
  trigger: string
): { ok: true; next: SellerState } | { ok: false; error: string } {
  const next = TRANSITIONS[current]?.[trigger];
  if (!next) return { ok: false, error: `Invalid transition: ${current} --[${trigger}]--> ?` };
  return { ok: true, next };
}
