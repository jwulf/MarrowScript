// Generated by MarrowScript compiler. DO NOT EDIT.

export type OrderState = "pending" | "paid" | "shipped" | "delivered" | "cancelled" | "refunded";

export const ORDER_INITIAL: OrderState = "pending";

const TRANSITIONS: Record<OrderState, Record<string, OrderState>> = {
  "pending": { "pending_to_paid": "paid" },
  "paid": { "paid_to_shipped": "shipped" },
  "shipped": { "shipped_to_delivered": "delivered" },
  "delivered": { "delivered_to_cancelled": "cancelled", "delivered_to_refunded": "refunded" },
  "cancelled": {  },
  "refunded": {  },
};

export function transitionOrder(
  current: OrderState,
  trigger: string
): { ok: true; next: OrderState } | { ok: false; error: string } {
  const next = TRANSITIONS[current]?.[trigger];
  if (!next) return { ok: false, error: `Invalid transition: ${current} --[${trigger}]--> ?` };
  return { ok: true, next };
}
