// Generated by MarrowScript compiler. DO NOT EDIT.
// Regression tests derived from capability declarations.
// Run: npx ts-node src/tests.ts

import * as http from "http";

const BASE_URL = process.env.TEST_BASE_URL || "http://localhost:3000";
const AUTH_TOKEN = process.env.TEST_AUTH_TOKEN || "";

let passed = 0;
let failed = 0;

async function request(method: string, path: string, body?: any): Promise<{ status: number; data: any }> {
  const url = new URL(path, BASE_URL);
  const res = await fetch(url.toString(), {
    method,
    headers: {
      "Content-Type": "application/json",
      ...(AUTH_TOKEN ? { "Authorization": `Bearer ${AUTH_TOKEN}` } : {}),
    },
    body: body ? JSON.stringify(body) : undefined,
  });
  const data = await res.json().catch(() => ({}));
  return { status: res.status, data };
}

async function test(name: string, fn: () => Promise<void>): Promise<void> {
  try {
    await fn();
    console.log(`  ✓ ${name}`);
    passed++;
  } catch (e: any) {
    console.log(`  ✗ ${name}: ${e.message}`);
    failed++;
  }
}

function assert(condition: boolean, message: string): void {
  if (!condition) throw new Error(message);
}

(async () => {
// ─── SellerService Tests ─────────────────────────────────────────────────────

console.log("\nSellerService — CRUD");

let __seller_id: string;

await test("POST /sellers — creates entity", async () => {
  const { status, data } = await request("POST", "/sellers", {"name":"test_value","email":"test_value","balance":1,"rating":1});
  assert(status === 201, `Expected 201, got ${status}: ${JSON.stringify(data)}`);
  assert(data.id, "Response must have id");
  __seller_id = data.id;
});

await test("GET /sellers/:id — reads entity", async () => {
  const { status, data } = await request("GET", `/sellers/${__seller_id}`);
  assert(status === 200, `Expected 200, got ${status}`);
  assert(data.id === __seller_id, "ID must match");
});

await test("GET /sellers — lists entities", async () => {
  const { status, data } = await request("GET", "/sellers");
  assert(status === 200, `Expected 200, got ${status}`);
  assert(Array.isArray(data.items), "Response must have items array");
  assert(typeof data.total === "number", "Response must have total");
});

await test("GET /sellers — rejects unauthenticated", async () => {
  const res = await fetch(`${BASE_URL}/sellers`);
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /sellers/publish-listing — capability executes", async () => {
  const { status, data } = await request("POST", "/sellers/publish-listing", {
    seller_id: __seller_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /sellers/publish-listing — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/sellers/publish-listing`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /sellers/archive-listing — capability executes", async () => {
  const { status, data } = await request("POST", "/sellers/archive-listing", {
    seller_id: __seller_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /sellers/archive-listing — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/sellers/archive-listing`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /sellers/ship-order — capability executes", async () => {
  const { status, data } = await request("POST", "/sellers/ship-order", {
    seller_id: __seller_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /sellers/ship-order — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/sellers/ship-order`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

// State machine: Seller
await test("PUT /sellers/:id — rejects invalid state transition", async () => {
  const { status, data } = await request("PUT", `/sellers/${__seller_id}`, {
    state: "__invalid_state__",
  });
  assert(status === 422, `Expected 422 for invalid transition, got ${status}`);
  assert(data.error?.code === "INVALID_TRANSITION", "Error code must be INVALID_TRANSITION");
});

await test("DELETE /sellers/:id — deletes entity", async () => {
  const { status } = await request("DELETE", `/sellers/${__seller_id}`);
  assert(status === 204, `Expected 204, got ${status}`);
});

await test("GET /sellers/:id — returns 404 after delete", async () => {
  const { status } = await request("GET", `/sellers/${__seller_id}`);
  assert(status === 404, `Expected 404, got ${status}`);
});

// ─── ListingService Tests ─────────────────────────────────────────────────────

console.log("\nListingService — CRUD");

let __listing_id: string;

await test("POST /listings — creates entity", async () => {
  const { status, data } = await request("POST", "/listings", {"seller_id":"00000000-0000-0000-0000-000000000001","title":"test_value","description":"test_value","price":1,"stock":1,"category":"test_value"});
  assert(status === 201, `Expected 201, got ${status}: ${JSON.stringify(data)}`);
  assert(data.id, "Response must have id");
  __listing_id = data.id;
});

await test("GET /listings/:id — reads entity", async () => {
  const { status, data } = await request("GET", `/listings/${__listing_id}`);
  assert(status === 200, `Expected 200, got ${status}`);
  assert(data.id === __listing_id, "ID must match");
});

await test("GET /listings — lists entities", async () => {
  const { status, data } = await request("GET", "/listings");
  assert(status === 200, `Expected 200, got ${status}`);
  assert(Array.isArray(data.items), "Response must have items array");
  assert(typeof data.total === "number", "Response must have total");
});

await test("GET /listings — rejects unauthenticated", async () => {
  const res = await fetch(`${BASE_URL}/listings`);
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /listings/publish-listing — capability executes", async () => {
  const { status, data } = await request("POST", "/listings/publish-listing", {
    listing_id: __listing_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /listings/publish-listing — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/listings/publish-listing`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /listings/archive-listing — capability executes", async () => {
  const { status, data } = await request("POST", "/listings/archive-listing", {
    listing_id: __listing_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /listings/archive-listing — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/listings/archive-listing`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /listings/place-order — capability executes", async () => {
  const { status, data } = await request("POST", "/listings/place-order", {
    listing_id: __listing_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /listings/place-order — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/listings/place-order`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

// State machine: Listing
await test("PUT /listings/:id — rejects invalid state transition", async () => {
  const { status, data } = await request("PUT", `/listings/${__listing_id}`, {
    state: "__invalid_state__",
  });
  assert(status === 422, `Expected 422 for invalid transition, got ${status}`);
  assert(data.error?.code === "INVALID_TRANSITION", "Error code must be INVALID_TRANSITION");
});

await test("DELETE /listings/:id — deletes entity", async () => {
  const { status } = await request("DELETE", `/listings/${__listing_id}`);
  assert(status === 204, `Expected 204, got ${status}`);
});

await test("GET /listings/:id — returns 404 after delete", async () => {
  const { status } = await request("GET", `/listings/${__listing_id}`);
  assert(status === 404, `Expected 404, got ${status}`);
});

// ─── BuyerService Tests ─────────────────────────────────────────────────────

console.log("\nBuyerService — CRUD");

let __buyer_id: string;

await test("POST /buyers — creates entity", async () => {
  const { status, data } = await request("POST", "/buyers", {"name":"test_value","email":"test_value","balance":1});
  assert(status === 201, `Expected 201, got ${status}: ${JSON.stringify(data)}`);
  assert(data.id, "Response must have id");
  __buyer_id = data.id;
});

await test("GET /buyers/:id — reads entity", async () => {
  const { status, data } = await request("GET", `/buyers/${__buyer_id}`);
  assert(status === 200, `Expected 200, got ${status}`);
  assert(data.id === __buyer_id, "ID must match");
});

await test("GET /buyers — lists entities", async () => {
  const { status, data } = await request("GET", "/buyers");
  assert(status === 200, `Expected 200, got ${status}`);
  assert(Array.isArray(data.items), "Response must have items array");
  assert(typeof data.total === "number", "Response must have total");
});

await test("GET /buyers — rejects unauthenticated", async () => {
  const res = await fetch(`${BASE_URL}/buyers`);
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /buyers/place-order — capability executes", async () => {
  const { status, data } = await request("POST", "/buyers/place-order", {
    buyer_id: __buyer_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /buyers/place-order — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/buyers/place-order`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /buyers/process-payment — capability executes", async () => {
  const { status, data } = await request("POST", "/buyers/process-payment", {
    buyer_id: __buyer_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /buyers/process-payment — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/buyers/process-payment`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /buyers/post-review — capability executes", async () => {
  const { status, data } = await request("POST", "/buyers/post-review", {
    buyer_id: __buyer_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /buyers/post-review — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/buyers/post-review`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

// State machine: Buyer
await test("PUT /buyers/:id — rejects invalid state transition", async () => {
  const { status, data } = await request("PUT", `/buyers/${__buyer_id}`, {
    state: "__invalid_state__",
  });
  assert(status === 422, `Expected 422 for invalid transition, got ${status}`);
  assert(data.error?.code === "INVALID_TRANSITION", "Error code must be INVALID_TRANSITION");
});

await test("DELETE /buyers/:id — deletes entity", async () => {
  const { status } = await request("DELETE", `/buyers/${__buyer_id}`);
  assert(status === 204, `Expected 204, got ${status}`);
});

await test("GET /buyers/:id — returns 404 after delete", async () => {
  const { status } = await request("GET", `/buyers/${__buyer_id}`);
  assert(status === 404, `Expected 404, got ${status}`);
});

// ─── OrderService Tests ─────────────────────────────────────────────────────

console.log("\nOrderService — CRUD");

let __order_id: string;

await test("POST /orders — creates entity", async () => {
  const { status, data } = await request("POST", "/orders", {"buyer_id":"00000000-0000-0000-0000-000000000001","listing_id":"00000000-0000-0000-0000-000000000001","seller_id":"00000000-0000-0000-0000-000000000001","quantity":1,"unit_price":1,"total":1,"status":"test_value"});
  assert(status === 201, `Expected 201, got ${status}: ${JSON.stringify(data)}`);
  assert(data.id, "Response must have id");
  __order_id = data.id;
});

await test("GET /orders/:id — reads entity", async () => {
  const { status, data } = await request("GET", `/orders/${__order_id}`);
  assert(status === 200, `Expected 200, got ${status}`);
  assert(data.id === __order_id, "ID must match");
});

await test("GET /orders — lists entities", async () => {
  const { status, data } = await request("GET", "/orders");
  assert(status === 200, `Expected 200, got ${status}`);
  assert(Array.isArray(data.items), "Response must have items array");
  assert(typeof data.total === "number", "Response must have total");
});

await test("GET /orders — rejects unauthenticated", async () => {
  const res = await fetch(`${BASE_URL}/orders`);
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /orders/process-payment — capability executes", async () => {
  const { status, data } = await request("POST", "/orders/process-payment", {
    order_id: __order_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /orders/process-payment — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/orders/process-payment`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /orders/ship-order — capability executes", async () => {
  const { status, data } = await request("POST", "/orders/ship-order", {
    order_id: __order_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /orders/ship-order — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/orders/ship-order`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /orders/cancel-order — capability executes", async () => {
  const { status, data } = await request("POST", "/orders/cancel-order", {
    order_id: __order_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /orders/cancel-order — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/orders/cancel-order`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /orders/post-review — capability executes", async () => {
  const { status, data } = await request("POST", "/orders/post-review", {
    order_id: __order_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /orders/post-review — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/orders/post-review`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

// State machine: Order
await test("PUT /orders/:id — rejects invalid state transition", async () => {
  const { status, data } = await request("PUT", `/orders/${__order_id}`, {
    state: "__invalid_state__",
  });
  assert(status === 422, `Expected 422 for invalid transition, got ${status}`);
  assert(data.error?.code === "INVALID_TRANSITION", "Error code must be INVALID_TRANSITION");
});

await test("DELETE /orders/:id — deletes entity", async () => {
  const { status } = await request("DELETE", `/orders/${__order_id}`);
  assert(status === 204, `Expected 204, got ${status}`);
});

await test("GET /orders/:id — returns 404 after delete", async () => {
  const { status } = await request("GET", `/orders/${__order_id}`);
  assert(status === 404, `Expected 404, got ${status}`);
});

// ─── ReviewService Tests ─────────────────────────────────────────────────────

console.log("\nReviewService — CRUD");

let __review_id: string;

await test("POST /reviews — creates entity", async () => {
  const { status, data } = await request("POST", "/reviews", {"order_id":"00000000-0000-0000-0000-000000000001","buyer_id":"00000000-0000-0000-0000-000000000001","seller_id":"00000000-0000-0000-0000-000000000001","rating":1,"comment":"test_value"});
  assert(status === 201, `Expected 201, got ${status}: ${JSON.stringify(data)}`);
  assert(data.id, "Response must have id");
  __review_id = data.id;
});

await test("GET /reviews/:id — reads entity", async () => {
  const { status, data } = await request("GET", `/reviews/${__review_id}`);
  assert(status === 200, `Expected 200, got ${status}`);
  assert(data.id === __review_id, "ID must match");
});

await test("GET /reviews — lists entities", async () => {
  const { status, data } = await request("GET", "/reviews");
  assert(status === 200, `Expected 200, got ${status}`);
  assert(Array.isArray(data.items), "Response must have items array");
  assert(typeof data.total === "number", "Response must have total");
});

await test("GET /reviews — rejects unauthenticated", async () => {
  const res = await fetch(`${BASE_URL}/reviews`);
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("DELETE /reviews/:id — deletes entity", async () => {
  const { status } = await request("DELETE", `/reviews/${__review_id}`);
  assert(status === 204, `Expected 204, got ${status}`);
});

await test("GET /reviews/:id — returns 404 after delete", async () => {
  const { status } = await request("GET", `/reviews/${__review_id}`);
  assert(status === 404, `Expected 404, got ${status}`);
});

console.log(`\n════════════════════════════════════════`);
console.log(`Results: ${passed} passed, ${failed} failed`);
console.log("═".repeat(40));
if (failed > 0) process.exit(1);
})().catch(e => { console.error(e); process.exit(1); });