# Generated by MarrowScript compiler.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: marrow-forge
  labels:
    app: marrow-forge
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: marrow-forge
  template:
    metadata:
      labels:
        app: marrow-forge
    spec:
      containers:
        - name: marrow-forge
          image: marrow-forge:1.0.0
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3000"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: marrow-forge-secrets
                  key: database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: marrow-forge-secrets
                  key: jwt-secret
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: marrow-forge-secrets
                  key: redis-url
                  optional: true
          livenessProbe:
            httpGet:
              path: /health/live
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: marrow-forge
spec:
  selector:
    app: marrow-forge
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
