// Generated by MarrowScript compiler. DO NOT EDIT.
// Regression tests derived from capability declarations.
// Run: npx ts-node src/tests.ts

import * as http from "http";

const BASE_URL = process.env.TEST_BASE_URL || "http://localhost:3000";
const AUTH_TOKEN = process.env.TEST_AUTH_TOKEN || "";

let passed = 0;
let failed = 0;

async function request(method: string, path: string, body?: any): Promise<{ status: number; data: any }> {
  const url = new URL(path, BASE_URL);
  const res = await fetch(url.toString(), {
    method,
    headers: {
      "Content-Type": "application/json",
      ...(AUTH_TOKEN ? { "Authorization": `Bearer ${AUTH_TOKEN}` } : {}),
    },
    body: body ? JSON.stringify(body) : undefined,
  });
  const data = await res.json().catch(() => ({}));
  return { status: res.status, data };
}

async function test(name: string, fn: () => Promise<void>): Promise<void> {
  try {
    await fn();
    console.log(`  ✓ ${name}`);
    passed++;
  } catch (e: any) {
    console.log(`  ✗ ${name}: ${e.message}`);
    failed++;
  }
}

function assert(condition: boolean, message: string): void {
  if (!condition) throw new Error(message);
}

(async () => {
// ─── ForgeRequestService Tests ─────────────────────────────────────────────────────

console.log("\nForgeRequestService — CRUD");

let __forge_request_id: string;

await test("POST /forge_requests — creates entity", async () => {
  const { status, data } = await request("POST", "/forge_requests", {"title":"test_value","description":"test_value","desired_kind":"test_value","target_path":"test_value","complexity":1});
  assert(status === 201, `Expected 201, got ${status}: ${JSON.stringify(data)}`);
  assert(data.id, "Response must have id");
  __forge_request_id = data.id;
});

await test("GET /forge_requests/:id — reads entity", async () => {
  const { status, data } = await request("GET", `/forge_requests/${__forge_request_id}`);
  assert(status === 200, `Expected 200, got ${status}`);
  assert(data.id === __forge_request_id, "ID must match");
});

await test("GET /forge_requests — lists entities", async () => {
  const { status, data } = await request("GET", "/forge_requests");
  assert(status === 200, `Expected 200, got ${status}`);
  assert(Array.isArray(data.items), "Response must have items array");
  assert(typeof data.total === "number", "Response must have total");
});

await test("GET /forge_requests — rejects unauthenticated", async () => {
  const res = await fetch(`${BASE_URL}/forge_requests`);
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /forge_requests/slice-codebase — capability executes", async () => {
  const { status, data } = await request("POST", "/forge_requests/slice-codebase", {
    forge_request_id: __forge_request_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /forge_requests/forge — capability executes", async () => {
  const { status, data } = await request("POST", "/forge_requests/forge", {
    forge_request_id: __forge_request_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /forge_requests/scope-request — capability executes", async () => {
  const { status, data } = await request("POST", "/forge_requests/scope-request", {
    forge_request_id: __forge_request_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /forge_requests/scope-request — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/forge_requests/scope-request`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /forge_requests/generate-request — capability executes", async () => {
  const { status, data } = await request("POST", "/forge_requests/generate-request", {
    forge_request_id: __forge_request_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /forge_requests/generate-request — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/forge_requests/generate-request`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("DELETE /forge_requests/:id — deletes entity", async () => {
  const { status } = await request("DELETE", `/forge_requests/${__forge_request_id}`);
  assert(status === 204, `Expected 204, got ${status}`);
});

await test("GET /forge_requests/:id — returns 404 after delete", async () => {
  const { status } = await request("GET", `/forge_requests/${__forge_request_id}`);
  assert(status === 404, `Expected 404, got ${status}`);
});

// ─── ForgeResultService Tests ─────────────────────────────────────────────────────

console.log("\nForgeResultService — CRUD");

let __forge_result_id: string;

await test("POST /forge_results — creates entity", async () => {
  const { status, data } = await request("POST", "/forge_results", {"request_id":"00000000-0000-0000-0000-000000000001","kind":"test_value","summary":"test_value","target_path":"test_value","target_symbol":"test_value","artifact":"test_value","plan":"test_value","confidence":1,"ast_compiles":true,"tier_used":"test_value","trace_id":"test_value"});
  assert(status === 201, `Expected 201, got ${status}: ${JSON.stringify(data)}`);
  assert(data.id, "Response must have id");
  __forge_result_id = data.id;
});

await test("GET /forge_results/:id — reads entity", async () => {
  const { status, data } = await request("GET", `/forge_results/${__forge_result_id}`);
  assert(status === 200, `Expected 200, got ${status}`);
  assert(data.id === __forge_result_id, "ID must match");
});

await test("GET /forge_results — lists entities", async () => {
  const { status, data } = await request("GET", "/forge_results");
  assert(status === 200, `Expected 200, got ${status}`);
  assert(Array.isArray(data.items), "Response must have items array");
  assert(typeof data.total === "number", "Response must have total");
});

await test("GET /forge_results — rejects unauthenticated", async () => {
  const res = await fetch(`${BASE_URL}/forge_results`);
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /forge_results/save-forge-result — capability executes", async () => {
  const { status, data } = await request("POST", "/forge_results/save-forge-result", {
    forge_result_id: __forge_result_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /forge_results/save-forge-result — returns 401 without auth", async () => {
  const res = await fetch(`${BASE_URL}/forge_results/save-forge-result`, { method: "POST" });
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /forge_results/mark-forge-failed — capability executes", async () => {
  const { status, data } = await request("POST", "/forge_results/mark-forge-failed", {
    forge_result_id: __forge_result_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

// State machine: ForgeResult
await test("PUT /forge_results/:id — rejects invalid state transition", async () => {
  const { status, data } = await request("PUT", `/forge_results/${__forge_result_id}`, {
    state: "__invalid_state__",
  });
  assert(status === 422, `Expected 422 for invalid transition, got ${status}`);
  assert(data.error?.code === "INVALID_TRANSITION", "Error code must be INVALID_TRANSITION");
});

await test("DELETE /forge_results/:id — deletes entity", async () => {
  const { status } = await request("DELETE", `/forge_results/${__forge_result_id}`);
  assert(status === 204, `Expected 204, got ${status}`);
});

await test("GET /forge_results/:id — returns 404 after delete", async () => {
  const { status } = await request("GET", `/forge_results/${__forge_result_id}`);
  assert(status === 404, `Expected 404, got ${status}`);
});

// ─── RepoForgeRequestService Tests ─────────────────────────────────────────────────────

console.log("\nRepoForgeRequestService — CRUD");

let __repo_forge_request_id: string;

await test("POST /repo_forge_requests — creates entity", async () => {
  const { status, data } = await request("POST", "/repo_forge_requests", {"title":"test_value","description":"test_value","preset":"test_value","repo_a_url":"test_value","repo_b_url":"test_value","repo_a_ref":"test_value","repo_b_ref":"test_value","target_path":"test_value","complexity":1});
  assert(status === 201, `Expected 201, got ${status}: ${JSON.stringify(data)}`);
  assert(data.id, "Response must have id");
  __repo_forge_request_id = data.id;
});

await test("GET /repo_forge_requests/:id — reads entity", async () => {
  const { status, data } = await request("GET", `/repo_forge_requests/${__repo_forge_request_id}`);
  assert(status === 200, `Expected 200, got ${status}`);
  assert(data.id === __repo_forge_request_id, "ID must match");
});

await test("GET /repo_forge_requests — lists entities", async () => {
  const { status, data } = await request("GET", "/repo_forge_requests");
  assert(status === 200, `Expected 200, got ${status}`);
  assert(Array.isArray(data.items), "Response must have items array");
  assert(typeof data.total === "number", "Response must have total");
});

await test("GET /repo_forge_requests — rejects unauthenticated", async () => {
  const res = await fetch(`${BASE_URL}/repo_forge_requests`);
  assert(res.status === 401, `Expected 401, got ${res.status}`);
});

await test("POST /repo_forge_requests/ingest-repo-a — capability executes", async () => {
  const { status, data } = await request("POST", "/repo_forge_requests/ingest-repo-a", {
    repo_forge_request_id: __repo_forge_request_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /repo_forge_requests/ingest-repo-b — capability executes", async () => {
  const { status, data } = await request("POST", "/repo_forge_requests/ingest-repo-b", {
    repo_forge_request_id: __repo_forge_request_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("POST /repo_forge_requests/forge-from-repos — capability executes", async () => {
  const { status, data } = await request("POST", "/repo_forge_requests/forge-from-repos", {
    repo_forge_request_id: __repo_forge_request_id,
  });
  // Capability may return 200 (success) or 422 (precondition failed) — both are valid
  assert([200, 422].includes(status), `Expected 200 or 422, got ${status}: ${JSON.stringify(data)}`);
});

await test("DELETE /repo_forge_requests/:id — deletes entity", async () => {
  const { status } = await request("DELETE", `/repo_forge_requests/${__repo_forge_request_id}`);
  assert(status === 204, `Expected 204, got ${status}`);
});

await test("GET /repo_forge_requests/:id — returns 404 after delete", async () => {
  const { status } = await request("GET", `/repo_forge_requests/${__repo_forge_request_id}`);
  assert(status === 404, `Expected 404, got ${status}`);
});

console.log(`\n════════════════════════════════════════`);
console.log(`Results: ${passed} passed, ${failed} failed`);
console.log("═".repeat(40));
if (failed > 0) process.exit(1);
})().catch(e => { console.error(e); process.exit(1); });