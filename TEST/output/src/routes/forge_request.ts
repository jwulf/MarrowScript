// Generated by MarrowScript compiler. DO NOT EDIT.
// Service: ForgeRequestService
import { Router, Request, Response } from "express";
import { v4 as uuid } from "uuid";
import { query, queryOne, execute, pool } from "../db";
import { eventBus } from "../events";
import { requireAuth, AuthContext } from "../auth";
import rateLimit from "express-rate-limit";
import { auditLog } from "../audit";
import { logger } from "../logger";
import { counter } from "../metrics";
import { ForgeRequestCreateSchema, ForgeRequestUpdateSchema } from "../schemas";
import * as __algorithms from "../algorithms";
const { shortestPath, topologicalSort, binarySearch, bipartiteMatching, roundRobin, weightedAverage, percentile, rankBy, consistentHash } = __algorithms as any;

export const forgeRequestsRouter = Router();

// Rate limiter from policy declaration
const __routeRateLimit = rateLimit({ windowMs: 60000, max: 30, standardHeaders: true, legacyHeaders: false });

// CREATE
forgeRequestsRouter.post("/", __routeRateLimit, requireAuth, async (req: Request, res: Response) => {
  // Validate the request body against the generated Zod schema.
  const __parsed = ForgeRequestCreateSchema.safeParse(req.body);
  if (!__parsed.success) {
    return res.status(400).json({ error: { code: "VALIDATION_FAILED", message: "Invalid request body", issues: __parsed.error.flatten() } });
  }
  const __body = __parsed.data;
  try {
    const id = uuid();
    const { title, description, desired_kind, target_path, complexity } = __body as any;
    const sql = `INSERT INTO forge_requests (id, title, description, desired_kind, target_path, complexity) VALUES ($1, $2, $3, $4, $5, $6) RETURNING *`;
    const rows = await query(sql, [id, title, description, desired_kind, target_path, complexity]);
    counter("entity.created", { entity: "ForgeRequest" });
    res.status(201).json(rows[0]);
  } catch (e: any) {
    res.status(400).json({ error: { code: "CREATE_FAILED", message: e.message } });
  }
});

// READ
forgeRequestsRouter.get("/:id", __routeRateLimit, requireAuth, async (req: Request, res: Response) => {
  try {
    const row = await queryOne(`SELECT * FROM forge_requests WHERE id = $1`, [req.params.id]);
    if (!row) return res.status(404).json({ error: { code: "NOT_FOUND", message: "Not found" } });
    res.json(row);
  } catch (e: any) {
    res.status(500).json({ error: { code: "DB_ERROR", message: e.message } });
  }
});

// LIST
forgeRequestsRouter.get("/", __routeRateLimit, requireAuth, async (req: Request, res: Response) => {
  try {
    const page = parseInt(req.query.page as string) || 1;
    const pageSize = Math.min(parseInt(req.query.page_size as string) || 50, 100);
    const offset = (page - 1) * pageSize;
    const rows = await query(`SELECT * FROM forge_requests ORDER BY created_at DESC LIMIT $1 OFFSET $2`, [pageSize, offset]);
    const [{ count }] = await query(`SELECT COUNT(*) as count FROM forge_requests`);
    res.json({ items: rows, total: parseInt(count), page, page_size: pageSize });
  } catch (e: any) {
    res.status(500).json({ error: { code: "DB_ERROR", message: e.message } });
  }
});

// UPDATE
const __forgeRequestsUpdatable = new Set<string>(["title","description","desired_kind","target_path","complexity"]);
forgeRequestsRouter.put("/:id", __routeRateLimit, requireAuth, async (req: Request, res: Response) => {
  // 1. Validate body shape and types via Zod (UpdateSchema is partial).
  const __parsed = ForgeRequestUpdateSchema.safeParse(req.body);
  if (!__parsed.success) {
    return res.status(400).json({ error: { code: "VALIDATION_FAILED", message: "Invalid request body", issues: __parsed.error.flatten() } });
  }
  // 2. Reject unknown columns (defense in depth — Zod would already strip them).
  const __unknown = Object.keys(req.body || {}).filter(k => !__forgeRequestsUpdatable.has(k));
  if (__unknown.length > 0) {
    return res.status(400).json({ error: { code: "UNKNOWN_FIELDS", message: `Unknown fields: ${__unknown.join(", ")}`, fields: __unknown } });
  }
  // 3. Use the Zod-parsed object as the update set.
  const fields: Record<string, unknown> = __parsed.data as Record<string, unknown>;
  if (Object.keys(fields).length === 0) {
    return res.status(400).json({ error: { code: "NO_FIELDS", message: "No updatable fields supplied" } });
  }
  const keys = Object.keys(fields);
  const sets = keys.map((k, i) => `${k} = $${i + 2}`).join(", ");
  const values = keys.map(k => fields[k]);
  const sql = `UPDATE forge_requests SET ${sets}, updated_at = NOW() WHERE id = $1 RETURNING *`;
  const rows = await query(sql, [req.params.id, ...values]);
  if (rows.length === 0) return res.status(404).json({ error: { code: "NOT_FOUND", message: "Not found" } });
  res.json(rows[0]);
});

// DELETE
forgeRequestsRouter.delete("/:id", __routeRateLimit, requireAuth, async (req: Request, res: Response) => {
  try {
    const count = await execute(`DELETE FROM forge_requests WHERE id = $1`, [req.params.id]);
    if (count === 0) return res.status(404).json({ error: { code: "NOT_FOUND", message: "Not found" } });
    res.status(204).send();
  } catch (e: any) {
    res.status(500).json({ error: { code: "DB_ERROR", message: e.message } });
  }
});

// CAPABILITY: slice_codebase
forgeRequestsRouter.post("/slice-codebase", __routeRateLimit, requireAuth, auditLog("slice_codebase", "ForgeRequest"), async (req: Request, res: Response) => {
  const auth: AuthContext = (req as any).auth;
  try {
    // sync: eventual — effects applied, events queued via outbox
    // Fetch entities
    const r = await queryOne(`SELECT * FROM forge_requests WHERE id = $1`, [req.body.r_id || req.params.id]);
    if (!r) {
      return res.status(404).json({ error: { code: "NOT_FOUND", message: "r not found" } });
    }

    res.json({ ok: true, action: "slice_codebase", entity: r });
  } catch (e: any) {
    if (!res.headersSent) {
      res.status(400).json({ error: { code: "CAPABILITY_FAILED", message: e.message } });
    } else {
      logger.error("late_error_after_response", { event: "slice_codebase", status: "rejected", metadata: { error: String(e?.message || e).slice(0, 256) } });
    }
  }
});

// CAPABILITY: forge [transactional declared, sql-tx skipped: pure cognition pipeline]
forgeRequestsRouter.post("/forge", __routeRateLimit, requireAuth, auditLog("forge", "ForgeRequest"), async (req: Request, res: Response) => {
  const auth: AuthContext = (req as any).auth;
  try {
    // Pipeline: 3 step(s), sequential
    let r: any;
    if (req.body && req.body.r_id) {
      r = await queryOne(`SELECT * FROM forge_requests WHERE id = $1`, [req.body.r_id]);
      if (!r) {
        return res.status(404).json({ error: { code: "NOT_FOUND", message: "r not found" } });
      }
    } else if (req.params && req.params.id) {
      r = await queryOne(`SELECT * FROM forge_requests WHERE id = $1`, [req.params.id]);
      if (!r) {
        return res.status(404).json({ error: { code: "NOT_FOUND", message: "r not found" } });
      }
    } else if (req.body && typeof req.body === "object") {
      // Inline mode: treat the body as the entity itself.
      r = req.body;
    } else {
      return res.status(400).json({ error: { code: "MISSING_INPUT", message: "Provide r_id or the r body" } });
    }
    const { randomUUID: __randomUUID } = require("crypto");
    const __cogCtx = { trace_id: __randomUUID() };
    const { getPrompt: __getPrompt } = require("../cognition");
    const { runCognition: __runCog } = require("../cognition");
    const __pipeline_completed: { step: string; rollback: (() => Promise<void>) | null }[] = [];
    const __pipeline_results: Record<string, unknown> = {};

    try {
      __pipeline_results["analysis"] = await __getPrompt("analyze_forge_request")({ title: r.title, description: r.description, desired_kind: r.desired_kind, target_path: r.target_path }, __cogCtx);
      __pipeline_completed.push({ step: "analyze_forge_request", rollback: null });
      counter("pipeline.step_completed", { method: "forge", step: "analyze_forge_request" });
      __pipeline_results["slice"] = await __runCog("semantic_slice", { task: r.description, max_files: 8, hop_depth: 2 });
      __pipeline_completed.push({ step: "slice_codebase", rollback: null });
      counter("pipeline.step_completed", { method: "forge", step: "slice_codebase" });
      __pipeline_results["artifact"] = await __getPrompt("generate_forge_artifact")({ plan: (__pipeline_results["analysis"] as any), target_symbol: "", files: (__pipeline_results["slice"] as any), desired_kind: r.desired_kind, target_path: r.target_path, description: r.description, complexity: r.complexity }, __cogCtx);
      __pipeline_completed.push({ step: "generate_forge_artifact", rollback: null });
      counter("pipeline.step_completed", { method: "forge", step: "generate_forge_artifact" });
      res.json({
        ok: true,
        action: "forge",
        trace_id: __cogCtx.trace_id,
        results: __pipeline_results,
      });
    } catch (__err: any) {
      // on_error: rollback completed steps in reverse order
      for (const c of [...__pipeline_completed].reverse()) {
        if (c.rollback) await c.rollback().catch(() => {});
      }
      counter("pipeline.failed", { method: "forge" });
      logger.error("pipeline_failed", { event: "forge", metadata: { error: __err.message } });
      throw __err;
    }
  } catch (e: any) {
    if (!res.headersSent) {
      res.status(400).json({ error: { code: "CAPABILITY_FAILED", message: e.message } });
    } else {
      logger.error("late_error_after_response", { event: "forge", status: "rejected", metadata: { error: String(e?.message || e).slice(0, 256) } });
    }
  }
});

// CAPABILITY: scope_request
forgeRequestsRouter.post("/scope-request", __routeRateLimit, requireAuth, auditLog("scope_request", "ForgeRequest"), async (req: Request, res: Response) => {
  const auth: AuthContext = (req as any).auth;
  try {
    // sync: eventual — effects applied, events queued via outbox
    // Fetch entities
    const r = await queryOne(`SELECT * FROM forge_requests WHERE id = $1`, [req.body.r_id || req.params.id]);
    if (!r) {
      return res.status(404).json({ error: { code: "NOT_FOUND", message: "r not found" } });
    }

    // Preconditions
    if (!(r?.complexity >= 0)) {
      return res.status(422).json({ error: { code: "PRECONDITION_FAILED", message: "r.complexity >= 0" } });
    }

    res.json({ ok: true, action: "scope_request", entity: r });
  } catch (e: any) {
    if (!res.headersSent) {
      res.status(400).json({ error: { code: "CAPABILITY_FAILED", message: e.message } });
    } else {
      logger.error("late_error_after_response", { event: "scope_request", status: "rejected", metadata: { error: String(e?.message || e).slice(0, 256) } });
    }
  }
});

// CAPABILITY: generate_request
forgeRequestsRouter.post("/generate-request", __routeRateLimit, requireAuth, auditLog("generate_request", "ForgeRequest"), async (req: Request, res: Response) => {
  const auth: AuthContext = (req as any).auth;
  try {
    // sync: eventual — effects applied, events queued via outbox
    // Fetch entities
    const r = await queryOne(`SELECT * FROM forge_requests WHERE id = $1`, [req.body.r_id || req.params.id]);
    if (!r) {
      return res.status(404).json({ error: { code: "NOT_FOUND", message: "r not found" } });
    }

    // Preconditions
    if (!(r?.complexity >= 0)) {
      return res.status(422).json({ error: { code: "PRECONDITION_FAILED", message: "r.complexity >= 0" } });
    }

    res.json({ ok: true, action: "generate_request", entity: r });
  } catch (e: any) {
    if (!res.headersSent) {
      res.status(400).json({ error: { code: "CAPABILITY_FAILED", message: e.message } });
    } else {
      logger.error("late_error_after_response", { event: "generate_request", status: "rejected", metadata: { error: String(e?.message || e).slice(0, 256) } });
    }
  }
});
